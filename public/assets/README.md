# Assets

Drop these brand images here before building the relevant sections:

- `child.png` — 6-character 3D cartoon boy (doctor / engineer / professional / scientist / businessman / student). Transparent background.
- `school.png` — Indian school building (red-brick, Gothic, tricolour flag).
- `kartheek.jpg` — Founder photo (placeholder until a real one is provided).

The frame sequences (hero + problem1–4) are generated into `public/frames/` by
`npm run extract-frames` and are already in place.
